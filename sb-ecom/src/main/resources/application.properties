spring.config.import=optional:file:.env[.properties]

spring.application.name=sb-ecom

STRIPE_SECRET_KEY=todo
VITE_BACK_END_URL=http://localhost:8080/api

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ecommerce}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:admin}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Alternative H2 Database (for development/testing)
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:test

# Project Configuration
project.image=${PROJECT_IMAGE_DIR:images/}

# JWT Configuration
spring.app.jwtSecret=${JWT_SECRET:mySecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm}
spring.app.jwtExpirationMs=${JWT_EXPIRATION_MS:300000000}
spring.ecom.app.jwtCookieName=${JWT_COOKIE_NAME:springBootEcom}

# Frontend Configuration
frontend.allowedOrigins.url=${FRONTEND_ALLOWED_ORIGINS:http://localhost:5173/}
image.base.url=${IMAGE_BASE_URL:http://localhost:8080/images}

# External API Keys
stripe.secret.key=${STRIPE_SECRET_KEY}

# Server Configuration (optional for AWS Elastic Beanstalk)
server.port=${SERVER_PORT:8080}

# Optional: Cookie Domain (for production subdomains)
spring.ecom.app.cookieDomain=${COOKIE_DOMAIN:}

# Debug/Development Logging (uncomment and set DEBUG_ENABLED=true in .env to enable)
#logging.level.org.springframework=${DEBUG_ENABLED:INFO}
#logging.level.org.hibernate.SQL=${SQL_DEBUG_ENABLED:INFO}
#logging.level.org.springframework.security=${SECURITY_DEBUG_ENABLED:INFO}
#logging.level.com.ecommerce.project=${DEBUG_ENABLED:INFO}

# JPA Debug Settings (controlled by environment variables)
spring.jpa.show-sql=${SQL_DEBUG_ENABLED:false}
spring.jpa.properties.hibernate.format_sql=${SQL_DEBUG_ENABLED:false}
